
/**
 * Archivo de exportación para mantener la compatibilidad con el código existente.
 * Este archivo simplemente re-exporta la clase DBHandler desde su nueva ubicación.
 */
import { DBHandler } from './db/DBHandler';

// Re-exportamos la clase para que el código existente siga funcionando sin cambios
export { DBHandler };
